import React from 'react';
import Pagination from '../pagination';

const index = () => {
    return (
        <div>
            Henry food
            <Pagination/>
        </div>
    );
};

export default index;